# Loaded through BASH_ENV, set in /etc/codex/config.toml.
if [ -n "${KO_SANDBOX_TOOLCHAIN_PATH-}" ]; then
    case ":${PATH}:" in
        *":${KO_SANDBOX_TOOLCHAIN_PATH}:"*) ;;
        *) PATH="${KO_SANDBOX_TOOLCHAIN_PATH}:${PATH}" ;;
    esac
    export PATH
fi
