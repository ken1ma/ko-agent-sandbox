# The mill project the security gate runs its mill rows in: one module, one test,
# nothing to build but the contract. `system` is the JVM the wrapper puts first on PATH;
# mill's default provisions its own through Coursier's index, which the profile denies.
mill-jvm-version: system
extends: ScalaModule
scalaVersion: 3.8.4
